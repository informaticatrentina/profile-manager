# This file map the custom dependencies in base.txt to a source code repository
# that permit the download and the build of the required version.
#  
# Usually this requires to put in the package a version that is not released on
# pypi.
#
# The reason of the fork is usually specified in the latest commits in the
# source repository.


# We need the fileupload js also
https://github.com/ahref/flask-bootstrap/tarball/55d9a1a#egg=flask-bootstrap-2.3.2.2.dev1

# WTF for i18n, that is not yet released
https://github.com/ahref/flask-wtf/tarball/b2d2a7e#egg=Flask_WTF-0.8.4-dev1
